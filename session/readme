根据配置，通过工厂函数反射的方式动态实例化，容器对象。
1、cache模块，在每个session中额外添加了一个时间戳字段，用于后续达到session最大数量后清空超时session，避免辣鸡数据不删除导致内存消耗过大。
2、memcached模块，实现单例模式连接池。
3、memcached用dict方式存储数据和redis用hash方式存储数据，均为序列化后存储。存储hash和存储序列化数据，哪个效率高，待测试。

